const prefix = 'api/';

const urls = {
  getCountries: `${prefix}settings/getcountries`,
  getProvinces: `${prefix}settings/getprovinces`,
  registerUser: `${prefix}registration`,
};

export { urls as UrlConstants };
